# CPA 知识库：向量 + 拓扑 技术栈设计

> **核心命题**：CPA 知识体系高度结构化，纯 embedding 只能做「长得像」的匹配，抓不住「推导」「引用」「对比」「因果」这些真正有价值的关系。用 AI（LLM）来梳理知识点之间的关系是正确方向。

---

## 一、CPA 知识的特殊性：为什么 embedding 不够

| CPA 知识特点 | 纯 embedding 的局限 | 需要什么 |
|-------------|-------------------|---------|
| **层级结构** | chunk 沦为平铺碎片，丢失 科目→章→节 的层级 | 树形结构索引 |
| **条文引用** | "根据 IAS 16 第 30 段" 这种引用链无法追溯 | 引用关系图谱 |
| **易混概念** | "权益法 vs 成本法" 语义相近但逻辑相反，相似度反而误导 | 对比/互斥关系标注 |
| **计算链** | 从 营业收入→毛利润→营业利润→净利润 的推导路径丢失 | 推导/计算关系链路 |
| **案例关联** | 知识点和真题案例脱节，"长投合并" 对应哪些真题？ | 知识点↔题目映射 |
| **多跳推理** | "资产减值损失如何影响递延所得税？" 需要 A→B→C 的链式推理 | 图遍历多跳查询 |

**一句话**：CPA 知识库不是「文档库」，是「知识图谱 + 推理引擎」。

---

## 二、整体技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                    用户交互层                                 │
│  自然语言问答  │  知识点导航  │  学习路径  │  真题关联        │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                    混合检索引擎                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 语义向量检索  │  │ 图谱关系查询  │  │ 社区主题检索  │      │
│  │ (ChromaDB)   │  │ (Neo4j)      │  │ (Leiden聚类)  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                           │                                  │
│              ┌────────────▼────────────┐                    │
│              │     融合排序 & 重排     │                    │
│              │   α×向量 + β×图 + γ×社区│                    │
│              └─────────────────────────┘                    │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                   AI 知识抽取层（核心增量）                    │
│                                                             │
│  ┌───────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │ 概念抽取   │  │ 关系抽取  │  │ 公式抽取  │  │ 层级解析  │ │
│  │ 会计科目   │  │ 推导/引用 │  │ 计算逻辑  │  │ 科目章节目 │ │
│  │ 准则条款   │  │ 对比/因果 │  │ 分录结构  │  │ 录树重建   │ │
│  │ 税法要素   │  │ 前提/包含 │  │ 数据流    │  │          │ │
│  └─────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘ │
│        └──────────────┴─────────────┴─────────────┘        │
│                           │                                  │
│              ┌────────────▼────────────┐                    │
│              │    LLM (DeepSeek/Qwen)  │                    │
│              │    Few-shot Prompt 工程  │                    │
│              └─────────────────────────┘                    │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                    文档预处理层                               │
│  PDF 解析 → 目录提取 → 语义分块 → 表格识别 → OCR（扫描版）    │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、技术栈选型明细

### 3.1 文档处理

| 需求 | 选型 | 理由 |
|------|------|------|
| PDF 解析 | **PyMuPDF (fitz)** | 支持目录提取、表格识别，速度快 |
| 扫描版 OCR | **PaddleOCR** | 中文识别准确率高，离线可用 |
| 表格抽取 | **Camelot / Tabula** | 专门处理 PDF 中的财务报表 |
| Markdown 处理 | Python 原生 | 教材笔记通常为 MD 格式 |
| 语义分块 | **自定义 LangChain splitter** | 按标题层级 + 语义边界切分 |

### 3.2 AI 模型层 —— 整个方案的核心引擎

> 这是回答你 "是不是需要 AI 来梳理知识点之间的关系" 的关键部分。

#### 为什么必须用 AI（LLM）做关系梳理？

```
传统方式（人工标注）：
  "长期股权投资" ←关联→ "投资收益"
  标注 1 条关系需要 30 秒，CPA 六科上万知识点，不可行。

AI 方式（LLM 抽取）：
  输入：一段教材原文
  输出：{实体列表, 关系列表, 公式, 引用链}
  1 次 API 调用处理 1 个 chunk，批处理跑完一整本书只需几分钟。
```

#### LLM 选型

| 模型 | 场景 | 优势 |
|------|------|------|
| **DeepSeek-V3** | 知识抽取主力 | 成本低（¥1/百万token），中文理解强，128K 上下文 |
| **Qwen3-235B** | 复杂关系推理 | 推理能力强，适合多跳关系判断 |
| **bge-large-zh-v1.5** | 向量嵌入 | 中文检索 SOTA，1024 维 |
| **本地部署选项** | 隐私场景 | Qwen2.5-7B 量化版，消费级显卡可跑 |

#### AI 在四个环节的具体作用

```yaml
# ===== 1. 概念/实体抽取 =====
Prompt:
  你是一位 CPA 知识图谱专家。请从以下教材片段中提取所有会计概念、
  准则条款、税目税率、计算公式和案例。

  输出 JSON 格式:
  {
    "entities": [
      {
        "name": "长期股权投资",
        "type": "会计科目",
        "category": "资产类",
        "definition": "...",
        "chapter": "第5章 长期股权投资",
        "key_attributes": ["成本法", "权益法", "减值测试"]
      }
    ]
  }

# ===== 2. 关系抽取 =====
Prompt:
  基于上一个实体列表，判断这些实体之间的关系。

  关系类型包括:
  - derives_from:   "A 的计算依赖 B"（如 递延所得税 依赖 暂时性差异）
  - references:     "A 引用准则 B"（如 存货计量 引用了 企业会计准则第1号）
  - contrasts_with: "A 与 B 容易混淆，需要对比"
  - prerequisite_of: "学 B 之前必须先懂 A"
  - composes:       "A 由 B1, B2, B3 组成"
  - leads_to:       "A 的变动会导致 B 变动"

  输出:
  {
    "relations": [
      {"source": "营业收入", "target": "毛利润", "type": "derives_from",
       "formula": "营业收入 - 营业成本 = 毛利润"},
      {"source": "成本法", "target": "权益法", "type": "contrasts_with",
       "difference": "成本法仅在收到股利时确认收益，权益法按持股比例确认"}
    ]
  }

# ===== 3. 计算/公式抽取 =====
Prompt:
  提取以下内容中的计算公式和分录逻辑：

  输出:
  {
    "formulas": [
      {
        "name": "递延所得税资产计算",
        "expression": "可抵扣暂时性差异 × 适用税率",
        "inputs": ["可抵扣暂时性差异", "适用税率"],
        "output": "递延所得税资产",
        "conditions": "预期未来有足够应纳税所得额"
      }
    ]
  }

# ===== 4. 易混概念识别 =====
Prompt:
  识别 CPA 考试中常见的易混概念对/组，并说明区分要点：

  输出:
  {
    "confusion_pairs": [
      {
        "concept_a": "资本化支出",
        "concept_b": "费用化支出",
        "distinction": "资本化计入资产成本分期摊销，费用化当期损益",
        "典型例题": "固定资产后续支出哪类资本化？"
      }
    ]
  }
```

### 3.3 存储层

| 组件 | 选型 | 用途 | 规模预估 |
|------|------|------|---------|
| **向量库** | ChromaDB | 语义检索，存储每段文本嵌入 | CPA 六科约 5-10 万条 chunk |
| **图数据库** | **Neo4j** | 存储实体-关系图谱 | 预估 3000+ 实体，8000+ 关系 |
| **文档库** | 本地文件 / MinIO | 原始 PDF/MD，按需取原文 | |
| **元数据** | SQLite | 科目、章节、知识点编号的映射表 | |

#### Neo4j 图 Schema 设计

```cypher
// 节点类型
(:Subject {name: "会计", code: "ACC"})
(:Chapter {name: "第5章 长期股权投资", number: 5})
(:Section {name: "5.2 长期股权投资的后续计量"})
(:Concept {name: "成本法", type: "会计处理方法"})
(:Standard {name: "企业会计准则第2号", issuer: "财政部"})
(:Formula {name: "投资收益计算", expression: "..."})
(:Question {id: "2024-ACC-05", type: "单选题", source: "2024年真题"})
(:TaxItem {name: "企业所得税", rate: "25%"})

// 关系类型
(:Concept)-[:BELONGS_TO]->(:Section)
(:Section)-[:BELONGS_TO]->(:Chapter)
(:Chapter)-[:BELONGS_TO]->(:Subject)
(:Concept)-[:DERIVES_FROM]->(:Formula)
(:Concept)-[:REFERENCES]->(:Standard)
(:Concept)-[:CONTRASTS_WITH]->(:Concept)
(:Concept)-[:PREREQUISITE_OF]->(:Concept)
(:Concept)-[:TESTED_BY]->(:Question)
(:Formula)-[:LEADS_TO]->(:Formula)
```

### 3.4 检索引擎

```
用户查询: "资产减值损失如何影响递延所得税？"
                    │
     ┌──────────────┼──────────────┐
     ▼              ▼               ▼
  向量检索        图遍历          社区匹配
  (语义相似)      (关系路径)      (主题聚类)
     │              │               │
  找到提到      资产减值损失      匹配到
  "减值""递延"  ──leads_to→       "所得税"
  的 chunk      暂时性差异        社区摘要
                 ──derives_from→
                 递延所得税
     │              │               │
     └──────────────┴───────────────┘
                    ▼
              融合排序: 图路径 > 社区 > 向量
                    ▼
              LLM 综合生成答案:
              "资产减值损失会减少会计利润，产生可抵扣暂时性差异，
               从而确认递延所得税资产..."
```

### 3.5 应用层

| 功能 | 技术实现 | 说明 |
|------|---------|------|
| 知识问答 | FastAPI + SSE 流式输出 | 基于混合检索的 QA |
| 知识点导航 | React + D3.js / ECharts | 知识图谱可视化，点击跳转 |
| 学习路径 | 图拓扑排序 | 从基础概念到高级应用的推荐路径 |
| 真题联动 | 实体链接 | "这题考了哪个知识点？还有哪些类似题？" |
| 易混对比 | 关系查询 | "帮我对比权益法和成本法" 直接从图取对比关系 |

---

## 四、CPA 知识图谱的独特设计

### 4.1 双层图结构

```
第一层：科目层级树
  会计
  ├── 第1章 总论
  ├── 第2章 存货
  ├── 第3章 固定资产
  │   ├── 3.1 初始计量
  │   │   └── 知识点: 外购固定资产入账价值
  │   └── 3.2 后续计量
  │       └── 知识点: 折旧方法（年限平均/双倍余额/年数总和）
  ├── ...
  └── 第30章 合并财务报表

第二层：跨章节关系网络
  [存货跌价准备] ──影响──→ [所得税费用（递延所得税）]
      ↑                        ↑
  第2章                     第19章 所得税
      │                        │
  [资产减值] ──类比──→ [固定资产减值] ──影响──→ [递延所得税资产]
  第8章                     第3章
```

这种双层结构的好处：**章节内问题查层级树，跨章节综合题走关系网**。

### 4.2 易混概念组的特殊处理

CPA 考试大量考察概念辨析，这是 embedding 的致命弱点（相似概念得分反而高）。

在知识图谱中，用 `CONTRASTS_WITH` 边显式标注易混关系，查询时自动触发对比：

```
用户: "资本化和费用化的区别"
系统:
  1. 图查询找到 (资本化支出)-[:CONTRASTS_WITH]->(费用化支出)
  2. 返回: 对比表格 + 典型场景 + 相关真题
```

### 4.3 计算链追踪

```
净利润的计算链路（图遍历即可获得）:

营业收入
  ──subtract→ 营业成本
  ──subtract→ 税金及附加
  ──subtract→ 期间费用
  ──add─────→ 其他收益
  ──add─────→ 投资收益
  = 营业利润
  ──add─────→ 营业外收入
  ──subtract→ 营业外支出
  = 利润总额
  ──subtract→ 所得税费用
  = 净利润

查询 "哪些因素影响净利润？"
→ 从 [净利润] 节点反向 BFS 遍历 2-3 跳
→ 获得所有上游影响因素
→ 比 embedding 检索更完整、更有逻辑
```

---

## 五、开发实施路线

### Phase 1: 基础准备（1-2 天）
- [ ] 收集 CPA 六科教材（PDF/Markdown）
- [ ] 搭建 ChromaDB 环境（已有 `vector-knowledge-base` skill）
- [ ] 部署 Neo4j（Docker 一键部署）
- [ ] 配置 LLM API（DeepSeek）

### Phase 2: AI 知识抽取（核心，3-5 天）
- [ ] 编写概念抽取 Prompt 模板（Few-shot，覆盖典型 CPA 概念）
- [ ] 编写关系抽取 Prompt 模板（6 种关系类型）
- [ ] 编写公式/分录抽取模板
- [ ] 批处理 pipeline：逐章处理 → LLM 抽取 → 人工抽检 → 入图

### Phase 3: 图谱构建（2-3 天）
- [ ] 章节层级树导入 Neo4j
- [ ] 实体-关系数据批量写入
- [ ] 实体对齐去重（同一概念在不同章节的不同表述统一）
- [ ] 社区检测（Leiden 算法自动发现跨章节主题聚类）

### Phase 4: 混合检索（3-4 天）
- [ ] 向量检索 + 图检索双路实现
- [ ] 融合排序策略（针对不同查询类型调权重）
- [ ] 多跳推理实现（图遍历 + LLM 验证）
- [ ] 易混概念自动对比

### Phase 5: 应用 & 评估（3-5 天）
- [ ] FastAPI 后端 + 简单前端
- [ ] 真题测试集评估（对比纯 embedding vs 混合方案）
- [ ] 知识点可视化（D3.js 力导向图）
- [ ] 迭代优化

---

## 六、关键设计决策对比

| 决策点 | 方案 A | 方案 B | 推荐 |
|--------|--------|--------|------|
| LLM 用云端还是本地 | DeepSeek API（低成本） | Qwen2.5-7B 本地部署 | **A**：初期用 API 快速验证，效果稳定后可选部署 |
| 图数据库 | Neo4j | NetworkX（内存图） | **A**：CPA 规模大，NetworkX 后期会吃力 |
| 嵌入模型 | bge-large-zh | m3e-large | **A**：中文效果公认最好 |
| 分块策略 | 固定 500 字 | 语义分块 | **B**：CPA 教材结构清晰，按标题/小节边界分块效果更好 |
| 知识抽取 | 全量自动 | 半自动（AI抽+人工校） | **B**：先用 AI 批量抽取，关键关系人工复核 |

---

## 七、成本和资源预估

| 资源 | 预估量 | 说明 |
|------|--------|------|
| LLM Token 消耗 | 约 500 万 input tokens/科 | 六科约 3000 万 tokens，DeepSeek 约 ¥30 |
| 向量存储 | ChromaDB 索引约 2-5GB | 六科约 5-10 万条 chunk |
| 图存储 | Neo4j 数据约 500MB-1GB | 3000+ 节点，8000+ 关系 |
| 开发时间 | 约 2 周（MVP） | Phase 1-4，不含前端美化 |

---

## 八、为什么这个方案能解决你的问题

| 你的痛点 | 这个方案如何解决 |
|---------|----------------|
| "embedding 太简单" | AI(LLM) 做深度关系抽取，embedding 退化为辅助检索手段 |
| "效果不够好" | 图遍历保证关系完整性，不会漏掉 key 推导步骤 |
| "知识点之间的关系" | 显式构建 6 种关系类型的关系图谱，可查询、可推理 |
| "跨章节综合题" | 社区检测自动发现跨章节的概念聚类，不再依赖碎片匹配 |

---

> **下一步**：确认文档来源和规模之后，我可以直接开始写 Phase 2 的知识抽取 pipeline 代码——AI 驱动的概念抽取 + 关系构建是整套方案最有价值的增量。
