# 向量+拓扑知识库搭建方案

> 核心思路：向量负责「像什么」，拓扑负责「和什么有关」——两者互补，解决传统 RAG 的碎片化问题。

---

## 一、传统 RAG 为什么效果不好

| 问题 | 原因 |
|------|------|
| 碎片化 | 每个 chunk 独立嵌入，丢失跨 chunk 关联 |
| 无全局理解 | 无法回答需要综合多文档信息的问题 |
| 浅层检索 | 仅靠余弦相似度，缺乏推理能力 |
| 忽略结构 | 文档层级、章节关系、实体关联全部丢失 |
| 缺乏跳转 | 无法做多跳推理（A→B→C 的链式问题） |

---

## 二、整体架构

```
┌──────────────────────────────────────────────────┐
│                  文档摄入层                        │
│  PDF/MD/TXT → 解析 → 分块 → 实体抽取 → 关系抽取    │
└──────────┬───────────────────────┬──────────────┘
           │                       │
           ▼                       ▼
    ┌─────────────┐        ┌──────────────┐
    │  向量索引     │        │   图谱索引    │
    │  ChromaDB/  │        │  Neo4j /    │
    │  Milvus     │        │  NetworkX   │
    │             │        │             │
    │ 语义相似度   │        │ 拓扑关系     │
    │ chunk嵌入   │        │ 实体-关系图  │
    └──────┬──────┘        └──────┬───────┘
           │                      │
           └──────────┬───────────┘
                      ▼
            ┌─────────────────┐
            │   混合检索引擎    │
            │  向量分 + 图分   │
            │  融合排序        │
            └────────┬────────┘
                     ▼
            ┌─────────────────┐
            │   查询处理器      │
            │  问题分解        │
            │  实体链接        │
            │  多跳推理        │
            │  答案合成        │
            └─────────────────┘
```

---

## 三、核心模块设计

### 3.1 文档摄入与预处理

```
原始文档
  → 格式解析 (PDF/Markdown/Word/TXT)
  → 保留结构信息 (标题层级、段落关系、表格、列表)
  → 语义分块 (而非固定长度切分)
  → 每块保留元数据: {来源文档, 章节路径, 前后块ID}
```

**关键：语义分块优于固定分块**
- 用 `[CLS]` token 的相似度变化检测语义边界
- 或直接用 LLM 判断自然断点
- 块大小：中文约 400-800 字，保持语义完整

### 3.2 向量索引层（语义维度）

```
技术选型: ChromaDB (轻量入门) → Milvus (生产级)

嵌入模型:
  中文推荐 → bge-large-zh-v1.5 / m3e-large
  多语言   → paraphrase-multilingual-MiniLM-L12-v2

存储内容:
  - chunk 文本 + 嵌入向量
  - 元数据: 来源、章节、实体列表
  - 父子关系: 小 chunk 检索 + 大 chunk 返回
```

### 3.3 图谱索引层（拓扑维度）⭐ 核心

这是和传统 RAG 最大的区别：

```
图谱结构:

┌─────────────────────────────────────┐
│           文档层级图 (Hierarchy)      │
│                                     │
│   文档 ──→ 章节1 ──→ 小节1.1         │
│     │        │         └→ chunk_A   │
│     │        └──→ 小节1.2            │
│     │                └→ chunk_B     │
│     └──→ 章节2 ──→ chunk_C          │
│                                     │
├─────────────────────────────────────┤
│          实体关系图 (Entity Graph)    │
│                                     │
│   [产品A] ──依赖──→ [组件X]          │
│     │                  │            │
│   所属              依赖             │
│     ↓                  ↓            │
│   [部门A]          [组件Y]           │
│     │                  │            │
│   负责              参考             │
│     ↓                  ↓            │
│   [员工甲] ←──同事── [员工乙]         │
│                                     │
├─────────────────────────────────────┤
│         概念关联图 (Concept Graph)    │
│                                     │
│   文档提到"微服务" ──关联── 提到"Docker"│
│   文档提到"Docker" ──关联── 提到"K8s"│
│   通过社区检测自动发现主题聚类         │
└─────────────────────────────────────┘
```

**构建步骤：**

1. **实体抽取**
   - 用 LLM 从每个 chunk 抽取命名实体（人名、组织、产品、技术术语等）
   - Schema: `{entity_name, entity_type, chunk_id, description}`

2. **关系抽取**
   - 用 LLM 判断同一文档内实体间的关系
   - Schema: `{source_entity, relation_type, target_entity, evidence_chunk}`

3. **跨文档实体对齐**
   - 同一实体的不同提及合并（用向量相似度 + LLM 判断）
   - 构建实体别名表

4. **社区检测**（GraphRAG 的核心）
   - 用 Leiden/Louvain 算法检测图社区
   - 每个社区 = 一个主题簇，对社区内容做摘要
   - 全局性问题 → 查社区摘要；局部问题 → 查具体 chunk

### 3.4 混合检索引擎

```
用户查询
    │
    ├──→ 向量检索: top-k 语义相似 chunks
    │         │
    │         └──→ 得分: cosine_sim(query_emb, chunk_emb)
    │
    ├──→ 图检索:
    │     1. 实体链接: 从 query 中提取实体 → 定位图谱节点
    │     2. 子图展开: 从实体节点出发，1-2 跳邻居展开
    │     3. 层级导航: 沿文档层级图向上（找上下文）或向下（找细节）
    │     4. 社区检索: 匹配相关社区的摘要
    │
    └──→ 融合排序:
          final_score = α × vector_score + β × graph_score
                       + γ × community_score + δ × hierarchy_bonus
```

**图增强的典型场景：**

| 查询类型 | 传统 RAG | 向量+拓扑 |
|---------|---------|----------|
| "X 和 Y 有什么关系？" | 分别找 X 和 Y 的 chunk，硬拼 | 图遍历找到最短路径 |
| "这个系统的整体架构是什么？" | 单 chunk 视角 | 社区摘要 + 层级展开 |
| "所有依赖组件 Z 的服务有哪些？" | 很难 | 图反向遍历 |
| "A 为什么会导致 B？" | 碎片化 | 沿因果路径多跳推理 |

---

## 四、技术选型建议

### 推荐方案一：轻量入门（适合验证和中小规模）

| 组件 | 选型 | 理由 |
|------|------|------|
| 向量库 | ChromaDB | 零配置，Python 原生 |
| 图存储 | NetworkX + JSON | 内存图，简单直接，万级节点够用 |
| 嵌入模型 | bge-small-zh / m3e-base | 中文效果好，轻量 |
| LLM | 调用已有 API | 用于实体抽取、关系抽取、答案合成 |
| 部署 | 单机 Python | 无需额外服务 |

### 推荐方案二：生产级

| 组件 | 选型 | 理由 |
|------|------|------|
| 向量库 | Milvus / Qdrant | 高性能、支持过滤、分布式 |
| 图存储 | Neo4j | 原生图数据库，Cypher 查询 |
| 嵌入模型 | bge-large-zh-v1.5 | 最高精度的中文嵌入 |
| LLM | DeepSeek / Qwen | 高性价比 |
| 编排 | LangChain / LlamaIndex | 成熟的 RAG 框架 |

---

## 五、实施路线图

### Phase 1: 基础向量库（已有基础）
- [x] ChromaDB 搭建（已有 `vector-knowledge-base` skill）
- [ ] 优化为语义分块
- [ ] 引入更好的中文嵌入模型

### Phase 2: 图谱构建（核心增量）
- [ ] 实现实体抽取 pipeline（LLM + prompt 工程）
- [ ] 实现关系抽取 pipeline
- [ ] 构建 NetworkX 知识图谱
- [ ] 实体对齐与去重

### Phase 3: 社区检测与摘要
- [ ] 实现 Leiden/Louvain 社区检测
- [ ] 对每个社区生成摘要（LLM 总结）
- [ ] 建立社区 ↔ chunk 映射

### Phase 4: 混合检索
- [ ] 实现向量 + 图双路检索
- [ ] 融合排序算法
- [ ] 查询分解（复杂问题拆成子问题）

### Phase 5: 评估与迭代
- [ ] 构建测试问题集
- [ ] 对比传统 RAG vs 向量+拓扑方案
- [ ] 指标：召回率、答案完整性、多跳推理准确率

---

## 六、关键设计决策待确认

在动手之前，有几个方向需要你确认：

1. **数据规模**：大概有多少文档？总字数？这决定是否需要用 Neo4j 还是 NetworkX 就够了。

2. **查询类型**：主要回答什么类型的问题？
   - 事实查询（"XX 是什么？"）
   - 关系查询（"X 和 Y 有什么关系？"）
   - 综合/全局查询（"整个系统是怎么设计的？"）
   - 多跳推理（"为什么 A 会导致 B？"）

3. **数据来源**：文档是 PDF、Markdown、腾讯文档、还是网页？

4. **部署偏好**：
   - 纯本地（隐私优先）
   - 云端 API（省心）
   - 混合（本地向量库 + 云端 LLM）

5. **优先场景**：你最想先解决传统 RAG 的哪个痛点？
   - 多跳推理不行？
   - 全局理解差？
   - 关系发现弱？

---

## 七、参考方案

可以参考这些成熟的实现思路：

- **Microsoft GraphRAG**: 实体+关系+社区检测+摘要，最完整但较重
- **LightRAG**: 更轻量的 GraphRAG 实现，检索效率更高
- **Nano-GraphRAG**: 极简版，适合快速验证
- **Kotaemon + GraphRAG**: 带 UI 的开源方案

---

> **下一步建议**：先确认上面的设计决策，然后我从 Phase 2 开始写代码——在现有向量库基础上，把实体抽取和图谱构建跑通。
