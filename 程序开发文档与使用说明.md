# 审计OCR工具 -- 开发文档与使用说明

## 一、项目概述

基于 Ollama 本地大模型的审计凭证 OCR 识别与风险分析工具。通过调用视觉语言模型识别图片中的票据信息，结合知识库向量检索进行风险判断，最终将结果分类保存并写入数据库。

### 核心流程

```
图片输入 -> OCR识别(Qwen3.5) -> JSON解析 -> 数据校验 -> 分类保存/写库/Excel
                     |失败
              DeepSeek修复JSON -> 重试机制
```

### 技术栈

- Python 3.10+
- Ollama 本地模型（Qwen3.5 视觉模型 + DeepSeek 修复模型 + Qwen3-Embedding 向量模型）
- PostgreSQL 16（数据存储）
- psycopg2（数据库驱动）
- pandas + openpyxl（Excel 输出）

---

## 二、项目结构

```
重构版/
├── main.py                                # 程序入口（CLI 菜单）
├── input_pic/                             # 待识别图片放入此目录
├── output/                                # 运行结果自动输出到此目录
│
├── data/
│   └── config.json                        # 外部配置文件
│
├── config/
│   ├── settings.py                        # 配置管理（单例模式，加载 config.json）
│   └── constants.py                       # 常量定义（186 种图片类型列表）
│
├── models/
│   └── schemas.py                         # 数据模型（默认 JSON 结构、深度合并工具）
│
├── services/
│   ├── ollama_client.py                   # Ollama API 调用封装
│   ├── ocr_engine.py                      # OCR 识别核心（主模型 + 修复模型）
│   ├── validator.py                       # 数据校验（日期/金额/信用代码）
│   ├── db_service.py                      # 数据库写入服务
│   └── embedding_service.py               # Embedding 向量生成 + 余弦相似度
│
├── utils/
│   ├── converter.py                       # 中文大写金额转数字
│   ├── file_ops.py                        # 文件操作（保存图片/信息文件）
│   ├── json_handler.py                    # 自定义 JSON 编码器 + 安全解析
│   └── logger.py                          # 日志系统（错误日志 + 重跑任务队列）
│
├── processors/
│   ├── batch_processor.py                 # 批量处理主逻辑
│   └── retry_processor.py                 # 失败任务重跑
│
└── knowledge/                             # 知识库目录
    ├── audit_knowledge_base.json          # JSON 格式知识库（7 条规则）
    ├── 审计凭证OCR智能识别与审验知识库.md          # Markdown 知识库（15 段）
    ├── 审计凭证OCR——全类别凭证风险评估手册.md      # Markdown 知识库（28 段）
    └── import_all_knowledge.py            # 知识库入库脚本（新增文件时在此添加）
```

---

## 三、环境准备

### 3.1 安装 Python 依赖

```bash
pip install requests pandas openpyxl psycopg2-binary pillow
```

### 3.2 安装并启动 Ollama

从 https://ollama.com 下载安装，然后拉取模型：

```bash
ollama pull qwen3.5:9b          # 主识别模型（视觉模型）
ollama pull deepseek-r1:8b      # JSON 修复模型
ollama pull qwen3-embedding:8b  # 向量嵌入模型
```

启动 Ollama 服务：

```bash
ollama serve
```

### 3.3 配置 PostgreSQL 16

确保 PostgreSQL 16 服务运行中，创建数据库：

```powershell
# 或通过程序自动创建（运行 setup_db.py）
python knowledge/setup_kb_table.py
```

### 3.4 初始化知识库

```bash
cd 重构版
python knowledge/import_all_knowledge.py
```

该脚本会解析 knowledge 目录下的 .md 和 .json 文件，分段后生成 embedding 向量并写入数据库。

---

## 四、配置文件说明

位置：`data/config.json`

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `OLLAMA_API_URL` | Ollama 服务地址 | `http://localhost:11434/api/chat` |
| `MAIN_MODEL` | 主识别模型 | `qwen3.5:9b` |
| `FIX_MODEL` | JSON 修复模型 | `deepseek-r1:8b` |
| `IMAGE_FOLDER` | 待处理图片文件夹路径 | `input_pic/`（项目目录内） |
| `EXCEL_OUTPUT` | Excel 结果输出路径 | `output/audit_ocr_result.xlsx`（项目目录内） |
| `OUTPUT_ROOT_FOLDER` | 分类结果图片保存根目录 | `output/分类结果/`（项目目录内） |
| `ERROR_LOG_SIMPLE` | 错误日志路径 | `output/ocr_error_simple.log`（项目目录内） |
| `RETRY_TASK_LOG` | 重跑任务日志路径 | `output/ocr_retry_tasks.log`（项目目录内） |
| `SUPPORTED_FORMATS` | 支持的图片格式 | `[.png, .jpg, .jpeg, .webp, .gif]` |
| `RETRY_TIMES` | 主模型重试次数 | `5` |
| `FIX_RETRY_TIMES` | 修复模型重试次数 | `3` |
| `PROCESS_RETRY_TIMES` | 保存文件重试次数 | `5` |
| `TIMEOUT` | API 请求超时（秒） | `300` |
| `EMBED_MODEL` | 向量嵌入模型 | `qwen3-embedding:8b` |
| `KNOWLEDGE_BASE_PATH` | 知识库源文件路径 | `./knowledge/audit_knowledge_base.json` |
| `DB_ENABLED` | 是否启用数据库存储 | `true` |
| `DB_HOST` | 数据库地址 | `localhost` |
| `DB_PORT` | 数据库端口 | `5432` |
| `DB_NAME` | 数据库名 | `audit_ocr` |
| `DB_USER` | 数据库用户 | `postgres` |
| `DB_PASSWORD` | 数据库密码 | `admin` |

---

## 五、数据库设计

### 5.1 audit_results（审计结果表）

存储每次 OCR 识别的完整审计结果。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | SERIAL PK | 自增主键 |
| `image_name` | VARCHAR(500) | 图片文件名 |
| `image_path` | TEXT | 图片原始路径 |
| `classified_save_path` | TEXT | 分类保存后的路径 |
| `audit_time` | TIMESTAMP | 审计执行时间 |
| `image_type` | VARCHAR(100) | 识别出的图片类型 |
| `invoice_code` | VARCHAR(200) | 发票代码 |
| `invoice_number` | VARCHAR(200) | 发票号码 |
| `date` | VARCHAR(50) | 凭证日期 |
| `total_amount` | VARCHAR(100) | 总金额 |
| `relevant_party` | VARCHAR(500) | 相关方名称 |
| `tax_rate` | VARCHAR(100) | 税率 |
| `tax_id` | VARCHAR(200) | 税号 |
| `bank_info` | VARCHAR(500) | 银行信息 |
| `serial_number` | VARCHAR(200) | 流水号 |
| `details` | TEXT | 明细信息 |
| `seal_type` | VARCHAR(200) | 印章类型 |
| `seal_number` | VARCHAR(200) | 印章编号 |
| `seal_clarity` | VARCHAR(100) | 印章清晰度 |
| `joint_seal` | VARCHAR(50) | 骑缝章完整性 |
| `unified_social_credit_code` | VARCHAR(50) | 统一社会信用代码 |
| `legal_person` | VARCHAR(200) | 法人 |
| `valid_period` | VARCHAR(200) | 有效期 |
| `asset_tag` | VARCHAR(200) | 资产标签 |
| `asset_name` | VARCHAR(200) | 资产名称 |
| `location` | VARCHAR(500) | 存放位置 |
| `quantity` | VARCHAR(100) | 数量 |
| `progress` | VARCHAR(200) | 工程进度 |
| `signer` | VARCHAR(200) | 签字人 |
| `approval_level` | VARCHAR(200) | 审批级别 |
| `attachment_complete` | VARCHAR(100) | 附件完整性 |
| `other_info` | TEXT | 其他信息 |
| `date_valid` | BOOLEAN | 日期有效性 |
| `amount_valid` | BOOLEAN | 金额有效性 |
| `code_format_valid` | BOOLEAN | 编码格式有效性 |
| `no_missing_field` | BOOLEAN | 无缺失字段 |
| `image_normal` | BOOLEAN | 图像正常 |
| `no_duplicate` | BOOLEAN | 无重复 |
| `consistent_info` | BOOLEAN | 信息一致 |
| `compliance` | BOOLEAN | 合规性 |
| `no_fraud` | BOOLEAN | 无欺诈 |
| `risk_rating` | VARCHAR(50) | 风险评级 |
| `risk_description` | TEXT | 风险说明 |
| `audit_conclusion` | VARCHAR(50) | 审计结论 |
| `audit_reason` | TEXT | 审计原因 |
| `full_json` | JSONB | 完整原始识别JSON |
| `batch_id` | VARCHAR(100) | 批次号 |
| `created_at` | TIMESTAMP | 创建时间 |
| `updated_at` | TIMESTAMP | 更新时间 |

索引：audit_time, image_type, risk_rating, audit_conclusion, batch_id, relevant_party

### 5.2 knowledge_base（知识库表）

存储审计知识库及对应的向量嵌入。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | SERIAL PK | 自增主键 |
| `category` | VARCHAR(200) | 分类名称 |
| `rule_id` | VARCHAR(50) | 规则编号（JSON 文件自带或 MD 自动生成 `MD-xxx`） |
| `rule_name` | VARCHAR(500) | 规则/段落标题 |
| `section_title` | VARCHAR(500) | 章节标题 |
| `content` | TEXT | 规则内容 |
| `risk_level` | VARCHAR(20) | 风险等级（高/中/低） |
| `embedding` | DOUBLE PRECISION[] | 4096 维向量 |
| `source` | VARCHAR(500) | 来源文件名 |
| `created_at` | TIMESTAMP | 创建时间 |

---

## 六、模块详细说明

### 6.1 config/settings.py

配置加载模块，采用单例模式。启动时自动读取 `data/config.json`，合并默认值后以属性方式访问。

```python
from config.settings import cfg
print(cfg.MAIN_MODEL)      # qwen3.5:9b
print(cfg.TIMEOUT)         # 300
print(cfg.DB_ENABLED)      # True
```

### 6.2 services/ollama_client.py

Ollama API 调用封装：

- `call_ollama_model(model_name, prompt, image_base64)` -- 调用聊天模型，每次调用前主动重置上下文
- `check_ollama_service()` -- 检查 Ollama 服务是否可用
- `check_models_available()` -- 获取已安装模型列表

### 6.3 services/ocr_engine.py

OCR 识别核心：

- `audit_ocr_recognize(image_path)` -- 完整审计流程：读取图片 -> 调用 Qwen3.5 主模型 -> JSON 解析 -> 数据校验 -> 金额转换
- `fix_json_with_deepseek(raw_content, image_path)` -- 主模型返回 JSON 格式错误时，调用 DeepSeek 修复

重试策略：
- 主模型调用失败：重试 5 次（间隔 5 秒）
- JSON 解析失败：立即调用 DeepSeek 修复
- DeepSeek 修复失败：重试 3 次（间隔 5 秒）

### 6.4 services/validator.py

数据校验工具：

- `is_valid_date(date_str)` -- 校验日期格式 YYYY-MM-DD 且不超过当前时间
- `is_valid_amount(amount_str)` -- 校验金额（数字/中文大写均可）
- `is_valid_credit_code(code)` -- 校验 18 位统一社会信用代码

### 6.5 services/db_service.py

数据库操作服务：

- `save_batch_results(results, batch_id)` -- 批量写入审计结果
- 自动映射中文业务字段名 -> 数据库列名
- 支持事务，单条失败不影响其他记录

### 6.6 services/embedding_service.py

向量嵌入服务：

- `get_embedding(text)` -- 调用 qwen3-embedding 模型生成 4096 维向量
- `cosine_similarity(vec_a, vec_b)` -- 计算两个向量的余弦相似度

### 6.7 utils/converter.py

中文大写金额转数字（兼容简体/繁体）：

```python
chinese_to_number("壹仟贰佰叁拾肆元伍角陆分")  # 1234.56
chinese_to_number("壹仟貳佰參拾肆圓伍角陸分")  # 1234.56
chinese_to_number("1234.56")                     # 1234.56
```

### 6.8 utils/logger.py

日志系统：

- `init_log_files()` -- 初始化日志文件
- `write_simple_error_log(error_type, image_path, reason)` -- 写入错误日志
- `add_retry_task(task_type, image_path, raw_content)` -- 添加重跑任务到队列
- `update_retry_task_status(image_path, task_type, status)` -- 更新重跑任务状态

### 6.9 utils/file_ops.py

文件操作：

- `sanitize_filename(filename)` -- 清理文件名中的非法字符
- `save_image_and_info(image_path, audit_result, index)` -- 按类型分类保存图片 + 生成信息文件
- `get_safe_value(d, key, default)` -- 安全获取字典值

### 6.10 processors/batch_processor.py

批量处理主逻辑，串联整个流程：

1. 初始化日志
2. 检查 Ollama 服务状态
3. 扫描图片文件夹
4. 逐张执行 OCR 识别
5. 分类保存图片和信息文件
6. 生成 Excel 报告
7. 写入 PostgreSQL（如启用）

### 6.11 processors/retry_processor.py

失败任务重跑：

- 读取 `ocr_retry_tasks.log` 中的待重跑任务
- `model_fail` 类型：重新执行完整 OCR 识别
- `json_error` 类型：直接调用 DeepSeek 修复 JSON

---

## 七、使用说明

### 7.1 日常运行

```bash
cd "D:\pythonpro\ollama项目\重构版"
python main.py
```

选择模式：
- 输入 `1`（默认）：执行完整审计
- 输入 `2`：重跑失败任务

程序会自动扫描 `input_pic/` 目录下的图片，识别后分类保存至 `output/分类结果/`，生成 Excel 至 `output/audit_ocr_result.xlsx`，并写入 PostgreSQL 数据库。

### 7.2 知识库管理

新增知识文件后入库：

1. 将 .md 或 .json 文件放入 `knowledge/` 目录
2. 编辑 `knowledge/import_all_knowledge.py`，在 `FILES` 列表中加入文件名
3. 运行入库脚本：

```bash
python knowledge/import_all_knowledge.py
```

脚本会自动按 Markdown 标题层级分块 -> 生成向量 -> 写入 `knowledge_base` 表。

### 7.3 数据库查询示例

```sql
-- 查看最新审计记录
SELECT image_name, image_type, risk_rating, audit_conclusion, audit_time
FROM audit_results
ORDER BY id DESC LIMIT 10;

-- 按批次查询
SELECT * FROM audit_results WHERE batch_id = '20260513_191737';

-- 风险统计
SELECT risk_rating, COUNT(*) FROM audit_results GROUP BY risk_rating;

-- 按图片类型统计
SELECT image_type, COUNT(*) FROM audit_results GROUP BY image_type ORDER BY COUNT(*) DESC;

-- 知识库相似度检索
-- 通过程序调用 embedding_service.cosine_similarity() 实现
```

### 7.4 配置切换

- **关闭数据库写入**：将 `data/config.json` 中 `DB_ENABLED` 设为 `false`，程序只生成 Excel
- **切换模型**：修改 `MAIN_MODEL`、`FIX_MODEL`、`EMBED_MODEL`
- **调整超时**：修改 `TIMEOUT`（单位：秒）

---

## 八、异常处理机制

### 8.1 重试策略

| 异常类型 | 处理方式 |
|----------|----------|
| 模型调用失败 | 重试 5 次，间隔 5 秒 |
| JSON 格式错误 | 调用 DeepSeek 修复 |
| DeepSeek 修复失败 | 重试 3 次，仍失败则使用兜底 JSON |
| 图片读取失败 | 记录错误，跳过该文件 |
| 文件保存失败 | 重试 5 次，间隔 5 秒 |
| 数据库写入失败 | 单条回滚，不影响其他记录 |

### 8.2 日志文件

| 文件 | 说明 |
|------|------|
| `ocr_error_simple.log` | 结构化错误日志，格式：[时间] | 错误类型 | 文件路径 | 原因 |
| `ocr_retry_tasks.log` | 重跑任务队列（JSON 数组），记录待重跑的任务 |

---

## 九、向量检索说明

Embedding 服务提供语义检索能力：

```python
from services.embedding_service import get_embedding, cosine_similarity
import psycopg2

# 生成查询向量
query_vec = get_embedding("发票日期过期了怎么办")

# 从数据库读取所有知识库向量
conn = psycopg2.connect(host="localhost", dbname="audit_ocr", ...)
cur = conn.cursor()
cur.execute("SELECT id, rule_name, content, embedding FROM knowledge_base WHERE embedding IS NOT NULL")

# 逐一计算相似度，取 top-k
results = []
for row in cur.fetchall():
    vec = list(row[3])  # double precision[] -> Python list
    sim = cosine_similarity(query_vec, vec)
    results.append((sim, row[0], row[1], row[2]))

results.sort(key=lambda x: x[0], reverse=True)
top3 = results[:3]
```

向量维度：4096（由 qwen3-embedding:8b 模型决定）

---

## 十、开发扩展指南

### 10.1 新增图片类型

编辑 `config/constants.py` 中的 `IMAGE_TYPES` 列表，新类型会被自动在 prompt 中传给模型。

### 10.2 新增数据库字段

1. 在 `audit_results` 表中新增列
2. 在 `services/db_service.py` 的 `COLUMN_MAP` 列表中新增映射
3. 在 `processors/batch_processor.py` 的 row 字典中添加对应键值

### 10.3 新增校验规则

在 `services/validator.py` 中新增校验函数，然后在 `services/ocr_engine.py` 的 `audit_ocr_recognize` 末尾调用。

### 10.4 自定义 Prompt

修改 `services/ocr_engine.py` 中 `audit_ocr_recognize` 函数的 `prompt` 变量。
